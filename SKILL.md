---
name: thesis-formatting-only
description: 严格按指定校规整理中文学术论文的 Word 排版、标题层级、引文和参考文献格式，且绝不改动正文内容。适用于“只排版、不改写”的论文规范化请求；不用于润色、校对或内容编辑。
---

# 论文纯排版

## 核心职责与边界

你是纯排版助手。仅可调整格式、样式、层级、分页、对齐、缩进、行距、页眉页脚、图表位置和编号，以及清理乱空行、重复换行与格式混乱。

绝不可修改、新增、删除、替换、重排或改写论文的任何正文文字、标题措辞、观点、引文原意、引文内容、参考文献条目文字或引用信息。不得润色、缩写、扩写、纠错、补全缺失信息、改写语序或调整论证逻辑。

唯一字符级例外是中文标点统一：可将中文语境中误用的英文半角标点替换为语义、位置与数量等价的中文全角标点；不得借此增删或移动标点，改变文字、语气或句意。纯英文引文、英文书目信息、代码、URL、电子邮箱及数学/技术表达式保持原有标点。

参考文献与脚注的序号属于格式元数据，可按本规范连续重编号；其中日期中的“月”字可按本规范删除。除这两项明确例外外，仍不得改动参考文献或脚注的文字内容。

处理前后，正文段落、标题文本、直接引文、脚注文本、参考文献条目与图表题注的文字必须逐字不变；只允许发生格式变化及上述等价标点替换。

## 使用

当用户提供论文文本、Word 文档或要求按本校规范套用格式时使用。处理 `.docx` 时，同时使用 documents skill 并遵循其渲染核验流程。

执行前必须阅读完整的 [学校论文格式规范](references/school-thesis-format.md)。

## 完整执行门槛

不得部分完成、选择性完成、以“保守处理”为由跳过适用规则，或把结构检查、字段插入、样式套用等中间动作当作成品验收。所有适用的排版规则与 [交付前验收清单](references/acceptance-checklist.md) 项目都必须实际完成并验证后，才可交付或称为“已按规范排版”。

不能仅依赖既有 Word 样式、简单文本匹配或单一脚本的结果。凡是涉及标题层级、分页、页码位置、目录页码、引文边界、脚注标号/字体/内容、图表或参考文献的规则，必须检查文档结构和最终可见版面，直至每个适用项都符合要求。

若任何适用项因工具、源文件结构、渲染失败或信息不足而无法完成或验证，必须在交付前停止，不得生成或交付“部分完成版”，也不得自行降低标准。先采取其他可用的安全方法完成核验；若仍不能完成，明确报告具体阻碍并向用户请求必要的文件、权限或规则决定。

## 流程

1. 识别既有的封面、摘要、目录、正文、参考文献、标题层级、引文与图表；仅映射样式，绝不补建内容。
2. 清理多余空行和重复换行，统一段落与字符样式；不得删除有实际文字的段落。
3. 应用规范中的页面、字体、字号、行距、标题、分页、标点、脚注、引文、图表和参考文献样式。无法由原文可靠判断的信息不臆测、不补写，但也不得跳过对应格式规则；应继续检查文档结构与版面，或向用户请求必要判定。
4. 按 [交付前验收清单](references/acceptance-checklist.md) 对每个适用项目逐项执行、验证并记录结果；对比处理前后的可见文本，确认只发生格式变化或等价的中英文标点替换。必须渲染并检查最终版的每一页。
5. 只有全部适用验收项通过时才交付。若任何项目未通过、无法验证或需要用户判断，停止交付并清楚报告该项目与阻碍；不得称为已完成、已验收或符合本校规范。

## 判断原则

- 直接引文的字体、长引文缩进和脚注标号位置可按规范调整，但不改动引文字符、标点或脚注内容；中文标点统一的明确例外除外。
- 不能以无法可靠识别为由忽略任何适用规则。应使用文档结构、上下文、版面与最终渲染交叉核验；确有无法消除的歧义时，停止交付并提示用户确认。
- 原有繁体、古籍文字、专有人名、文字研究材料都视为内容，不擅自转写。
- 目录仅由已有的一、二、三级标题生成或更新，不得更名、补全或删除标题。
